import React from 'react';
import PropTypes from 'prop-types';

const SignUp = (props) => {
  return (
    <div>
      Hello
    </div>
  );
};

SignUp.propTypes = {};

SignUp.defaultProps = {};

export default SignUp;
